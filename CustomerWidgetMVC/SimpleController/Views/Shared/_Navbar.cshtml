@{
    Layout = null;
}
<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
        <a class="navbar-brand" href="./Register">Customer Widget</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbar" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse align-items-left justify-content-end" id="navbar">
            <ul class="navbar-nav">
                @{
                    if (Session["Email"] != null)
                    {
                        @*<div class="d-flex mr-auto align-items-center justify-content-center">*@
                <li class="list-item">
                    <h5 style="font-weight: 400" class="mr-3 mt-1">@Session["FirstName"]</h5>
                </li>
                <li class="list-item">
                    <a class="btn btn-outline-danger" style="width: 100px" id="btnlogout" href="@Url.Action("Logout", "Customer")">Log Out</a>
                </li>        
                @*</div>*@
                    }
                }
            </ul>
        </div>
    </div>
    @*@{
        if (Session["Email"] != null)
        {
        <div class="d-flex mr-auto align-items-center justify-content-center">
            <h5 style="font-weight: 400" class="mr-3 mt-1">@Session["FirstName"]!</h5>
            <a class="btn btn-outline-danger" style="width: 100px" id="btnlogout" href="@Url.Action("Logout", "Customer")">Log Out</a>
        </div>
        }
    }*@


</nav>


<style>
    li:hover {
        color: #000;
        font-weight: 500;
    }

    li {
        margin: 2px;
    }
</style>
